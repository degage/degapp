{
  "name": "@degage/update-address-coordinates",
  "version": "0.0.0",
  "main": "dist/update-address-coordinates.js",
  "scripts": {
    "babel-version": "babel --version",
    "develop": "nodemon --ext js,yml,json,sql --exec babel-node --debug src/update-address-coordinates --watch src",
    "start": "node .",
    "pretest": "npm run clean:test",
    "posttest": "nsp check",
    "test": "nyc --all --require babel-register --check-coverage --lines 50 ava --serial test/integration/setup.js",
    "prereport": "npm run clean:report",
    "report": "nyc report --reporter=html",
    "prebuild": "shx rm -rf dist",
    "build": "shx cp -r src dist && babel src -d dist",
    "lint": "standard --verbose 'src/**/*.js' 'scripts/**/*.js' | snazzy",
    "clean": "run-p clean:*",
    "clean:test": "shx rm -rf .nyc_output",
    "clean:report": "shx rm -rf coverage",
    "clean:build": "shx rm -rf dist"
  },
  "description": "",
  "repository": "",
  "private": true,
  "src": "src",
  "test": "test",
  "dist": "dist",
  "mainInput": "main",
  "mainOutput": "main",
  "dependencies": {
    "async": "^1.4.2",
    "babel-runtime": "^6.6.1",
    "body-parser": "^1.14.1",
    "expect": "^1.16.0",
    "express": "^4.13.4",
    "ioredis": "^2.4.0",
    "lodash": "^4.2.1",
    "moment": "^2.13.0",
    "mysql": "^2.10.2",
    "q": "^1.4.1",
    "request": "^2.0.5",
    "request-promise": "^4.1.1",
    "toobusy-js": "^0.4.3",
    "xhr": "^2.2.0",
    "xhr2": "^0.1.3"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-core": "^6.9.1",
    "babel-eslint": "^6.0.4",
    "babel-plugin-transform-runtime": "^6.8.0",
    "babel-polyfill": "^6.9.1",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-stage-0": "^6.5.0",
    "babel-register": "^6.9.0",
    "nodemon": "^1.9.2",
    "shx": "^0.1.2"
  }
}
